[project]
name = "news-topic-classifier"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "torch",
    "transformers",
    "gradio",
    "datasets",
    "evaluate",
    "scikit-learn",
    "accelerate",
    "numpy",
    "matplotlib",
    "seaborn",
]
