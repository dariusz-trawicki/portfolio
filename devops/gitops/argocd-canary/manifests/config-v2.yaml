apiVersion: v1
kind: ConfigMap
metadata:
  name: web-config-v2
data:
  default.conf: |
    server {
      listen 80;
      location /healthz { return 200 'ok\n'; }
      location / { return 200 'version 2\n'; }
    }
